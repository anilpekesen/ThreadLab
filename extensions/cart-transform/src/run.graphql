# Shopify, fonksiyon girdi sorgusuna 30 karmaşıklık sınırı koyuyor.
#
# Fiyat güvenliği: ürün fiyatı artık sepetteki "_base_unit_price" alanından
# DEĞİL satırın gerçek fiyatından (cost) alınır; baskı ücreti ürünü ve ücretin
# alt sınırı uygulamanın yazdığı $app:printlab/pricing metafield'ından gelir.
# Sepet alanları müşteri tarafından değiştirilebildiği için fiyat onlara
# bırakılamaz. Yer açmak için yalnız bilgi amaçlı beş alan çıkarıldı
# (toplam adet/ara toplam/toplam, toplu indirim, baskı indirimi).
query RunInput {
  cart {
    lines {
      id
      designRole: attribute(key: "_design_role") { value }
      surchargeUnit: attribute(key: "_surcharge_unit_total") { value }
      designToken: attribute(key: "_design_token") { value }
      designTokenAlt: attribute(key: "design_token") { value }
      designDetailUrl: attribute(key: "_design_detail_url") { value }
      locale: attribute(key: "_pl_locale") { value }
      frontDesign: attribute(key: "_pl_front_design") { value }
      frontDesignLabel: attribute(key: "Ön Tasarım") { value }
      backDesign: attribute(key: "_pl_back_design") { value }
      frontPrintUrl: attribute(key: "_front_print_url") { value }
      productUnitPrice: attribute(key: "_pl_product_unit_price") { value }
      frontSize: attribute(key: "_pl_front_size") { value }
      frontPrintPrice: attribute(key: "_pl_front_print_price") { value }
      frontPriceBand: attribute(key: "_pl_front_price_band") { value }
      frontPrintCount: attribute(key: "_pl_front_print_count") { value }
      frontPrintBreakdown: attribute(key: "_pl_front_print_breakdown") { value }
      backSize: attribute(key: "_pl_back_size") { value }
      backPrintPrice: attribute(key: "_pl_back_print_price") { value }
      backPriceBand: attribute(key: "_pl_back_price_band") { value }
      backPrintCount: attribute(key: "_pl_back_print_count") { value }
      backPrintBreakdown: attribute(key: "_pl_back_print_breakdown") { value }
      cost {
        amountPerQuantity { amount }
      }
      merchandise {
        ... on ProductVariant {
          id
          product {
            pricing: metafield(namespace: "$app:printlab", key: "pricing") { jsonValue }
          }
        }
      }
    }
  }
}

query RunInput {
  cart {
    lines {
      id
      quantity
      cost {
        totalAmount { amount }
      }
      unitPriceWithSurcharge: attribute(key: "_unit_price_with_surcharge") {
        value
      }
      merchandise {
        ... on ProductVariant {
          id
        }
      }
    }
  }
}

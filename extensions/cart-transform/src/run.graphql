query RunInput {
  cart {
    lines {
      id
      designRole: attribute(key: "_design_role") {
        value
      }
      surchargeTotal: attribute(key: "_surcharge_total") {
        value
      }
    }
  }
}
